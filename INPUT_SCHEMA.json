{
    "title": "Hello API Wrapper",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "name": {
            "title": "Name",
            "type": "string",
            "description": "Who should we greet?",
            "default": "world",
            "editor": "textfield"
        },
        "repeat": {
            "title": "Repeat",
            "type": "integer",
            "description": "How many messages to generate (1–50).",
            "default": 3,
            "minimum": 1,
            "maximum": 50,
            "editor": "number"
        },
        "echoInput": {
            "title": "Echo input",
            "type": "boolean",
            "description": "Include full input in each output row (useful for debugging).",
            "default": false,
            "editor": "checkbox"
        }
    },
    "required": []
}